# Not needed for the single-Vercel deployment (frontend and API share the
# same domain, so relative "/api/..." paths just work). Only set this if
# you host the Flask API somewhere separate from the frontend, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
